name: 'bacen_ifdata_analytics'
version: '0.0.1'
config-version: 2

profile: 'bacen_ifdata'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  bacen_ifdata_analytics:
    # Config keys by path
    gold:
      +materialized: table
